{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "shell": "bash",
            "command": "if [ -f package.json ]; then npm run type-check || exit 2; fi",
            "statusMessage": "Type-check gate before commit…",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
